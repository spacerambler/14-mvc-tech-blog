const router = require('express').Router();
const { Post, User } = require('../models');

// Create new user

// Login user

// Logout user

// update user

// Find all users

// find one user

// delete user

module.exports = router;