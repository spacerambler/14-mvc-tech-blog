{{#each posts as |post| }}
<div class="row mb-4 post">
  <div class="col-md-5">
    <h2>
      {{{title}}}
      <a href="/posts/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>
      {{post.postCopy}}
    </p>
  </div>
  <div class="col-md-7">
  </div>
</div>
{{/each}}